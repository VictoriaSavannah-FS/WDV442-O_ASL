//// This loads in all of the contacts data 
// directly from the library. It is a simple
// array of Contact objects.
const { ContactModel } = require('@jworkman-fs/asl');

// Exporting "ContactModel" -->to be used & refernced in our controllers

module.exports = ContactModel;
